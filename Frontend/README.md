# Tixora — Frontend

Dashboard frontend for the Tixora support CRM app.

## Setup

1. `npm install`
2. `npm run dev` to start the Vite dev server (port 3000)
3. `npm run build` to produce a production bundle in `dist/`
4. `npm run lint` to run ESLint
